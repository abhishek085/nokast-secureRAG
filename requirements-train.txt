# Fine-tuning deps. Installed on top of the vllm/vllm-openai:nightly-aarch64
# image (which already provides a CUDA-enabled torch + transformers for GB10).
peft>=0.13.0
trl>=0.12.0
datasets>=3.0.0
accelerate>=1.0.0
